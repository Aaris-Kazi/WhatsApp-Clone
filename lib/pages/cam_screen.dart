import 'package:flutter/material.dart';

class Camscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: Text('CamScreen'),
    );
  }
}